#ifndef HANDLE_COLORS_H
# define HANDLE_COLORS_H

short		handle_colors(const char *color, t_flag *flag, unsigned short *j);
int		pre_handle_colors(const char *format, t_flag *flag, unsigned short *j);

#endif